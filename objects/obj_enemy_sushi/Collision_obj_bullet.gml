/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

if (other.sniper <= 0)
	instance_destroy(other);
else
	other.sniper -= 1;

sprite_index = spr_something_dmg
alarm[2] = 4

//direction = random(360);

fire = other.fire;
freeze = other.freeze;

life -= 2;

if (fire > 0)
	alarm[1] = 1